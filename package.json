{
  "name": "sabah-buskers-mobile",
  "version": "1.7.0-beta",
  "description": "Sabah Buskers Community mobile app (Capacitor wrapper)",
  "private": true,
  "scripts": {
    "build:web": "node scripts/build-web.js",
    "sync": "npm run build:web && cap sync android",
    "build:apk": "npm run build:web && cap sync android && cd android && ./gradlew assembleDebug"
  },
  "devDependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/cli": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "typescript": "^7.0.2"
  }
}
